# include<bits/stdc++.h>
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/hash_policy.hpp>
using namespace std;

# define N 50005
__gnu_pbds::cc_hash_table<int, int> hash;
int T, n, a[N];

signed main() {
  cin >> T;
  while (T--) { 
    scanf ("%d", &n);
    for (int i = 1; i <= n; ++i) 
      scanf ("%d", &a[i]), ++hash[a[i]];
    for (int i = 1; i <= n; ++i) 
      if (hash[a[i]]) 
        printf ("%d", a[i]), hash[a[i]] = 0;
  }
}
